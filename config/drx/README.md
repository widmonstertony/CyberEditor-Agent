# DRX preset folder / DRX 预设目录

Export any required grades from DaVinci Resolve and place them here with one
of the following exact names:

- `interview_clean.drx`
- `cinematic.drx`
- `low_light_cleanup.drx`

请从 DaVinci Resolve 导出所需调色，并使用以上固定文件名放入本目录。项目不会
下载或伪造 DRX；AI 只能选择这些逻辑名称，执行器也只会读取本目录内的文件。
